apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-collector.fullname" . }}
  labels:
    {{- include "rust-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5678
      protocol: TCP
      name: http
  selector:
    {{- include "rust-collector.selectorLabels" . | nindent 4 }}
